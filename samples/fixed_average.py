# Student submission (revised after feedback)

def average(values):
    return sum(values) / len(values)


readings = [12.5, 13.1, 12.9, 13.4]
print("Average reading:", average(readings))
print("Number of readings:", len(readings))
